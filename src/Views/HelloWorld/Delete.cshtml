@using MlNetCore.Models.Views;
@model  MovieViewModel

@using (Html.BeginForm("Delete" ,"HelloWorld", FormMethod.Post, new { @class = "form-horizontal"}))
{
    <h4>Are you sure you want to delete this movie?</h4>
    @Html.HiddenFor(model => model.Id)
    <div class="form-group">
        <div class="col-md-off-set-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Yes" />
        </div>
    </div>
    
}